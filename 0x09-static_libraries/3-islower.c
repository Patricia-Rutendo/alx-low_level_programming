#include "main.h"
/**
 * _islower - checks for lowercase letters
 * @l: the character to check
 * Return: 1 if l is a lowercase, 0 if l is not a lowercase
 */
int _islower(int l)
{
	return (l >= 'a' && l <= 'z');
}

